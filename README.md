"# CobrApp" 
docker-compose up --build
instalar depeemdecia para encender:pip install fastapi uvicorn python-multipart---python -m pip install pytesseract opencv-python Pillow numpy  
y encenderlo:python -m uvicorn main:app --reload